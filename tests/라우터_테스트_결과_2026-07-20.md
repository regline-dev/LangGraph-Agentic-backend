# 규칙 라우터 테스트 (지시문 기반) — 21개 + 추가 2개 라이브 실행 결과

- 실행 방식: 실제 서버(`app.main:app`, Windows venv, port 8010)를 띄우고 `/agent/chat`에 직접 POST — 코드 수정 없이 관찰만
- 기준 코드: `app/graph/runtime.py` (mbti_command → catalog → fun_rank → verbatim → metric → title_card → vague → Groq Agent 순)
- 세션: 케이스별 독립 UUID 사용 (9→9b, 10→10b만 같은 세션으로 멀티턴 재현). 그 외는 "맥락 없음"을 정확히 보기 위해 매번 새 세션.
- 판단 기준(지시문 그대로): 응답에 `citations`가 있고 `snippet`이 문서 그대로면 검색/문서 경로, 없으면 규칙 라우터(되묻기/메타) 경로. `기대이하` = 원문/지표 요청인데 엉뚱한 검색을 탔거나, 명백한 의도인데 일반론 답변이 나온 경우.

이 문서는 **의견/제안만** 담는다. 코드는 건드리지 않았다.

================================================================================

## 결과 (21개 원본 + 2개 후속 턴)

1. "늑대와 어린양 내용평가는" : 정상
   [규칙] metric — content_eval (`app/metrics/route.py`, `detect.py` "내용평가" 키워드)
   [원인] 제목+지표 키워드 → Qdrant 메타 조회, LLM 없음
   [상태] pass

2. "박쥐와 족제비 원문 보여줘" : 정상
   [규칙] verbatim — origin (`verbatim.py` "원문" 트리거)
   [원인] 제목+origin 키워드 → 문서 본문 그대로 반환
   [상태] pass

3. "아버지와 아들들 줄거리 알려줘" : 정상
   [규칙] verbatim — origin ("줄거리" 트리거)
   [원인] 제목+origin 키워드 → 문서 본문 그대로
   [상태] pass

4. "박쥐" : 정상
   [규칙] vague — `_is_too_vague` (compact 길이 ≤4)
   [원인] "박쥐" ≠ 등록 제목 "박쥐와 족제비" 전체 문자열 → 되묻기
   [상태] pass

5. "해석" : 정상
   [규칙] vague — `_is_standalone_intent` ({"해석","결론","mbti"})
   [원인] 단독 의도어, 대상 없음 → 예시 되묻기
   [상태] pass

6. "결론" : 정상
   [규칙] vague — `_is_standalone_intent`
   [원인] 5번과 동일 계열
   [상태] pass

7. "재미도" (맥락 없음) : 정상
   [규칙] metric — fun (`detect.py` "재미도" 키워드), 제목/메모리 없음 → 되묻기
   [원인] metric 라우터가 vague보다 먼저 실행되고, 제목도 세션 메모리도 없어 "어떤 우화의 재미도를 알고 싶으신가요?"로 안전하게 되물음
   [상태] pass

8. "내용평가는" (맥락 없음) : 정상
   [규칙] metric — content_eval, 제목/메모리 없음 → 되묻기
   [원인] 7번과 동일. **지시문이 명시한 "맥락 없음 → 되묻기" 기준을 정확히 통과** (예전 사람 테스트 3번 "평가는"은 직전 대화가 있어 최근 제목으로 답했던 것과 대조됨 — 둘 다 정상 동작)
   [상태] pass

9. "늑대와 어린양 얘기하고" : 정상 ✅ (과거 [P0 환각] 항목 — 지금은 해결 확인됨)
   [규칙] title_card — `try_handle_title_only_full_card` (제목+"얘기하고" → 풀카드)
   [원인] 제목이 known_titles와 완전 일치 → LLM 없이 카드(원문+한마디+평가+키워드+최종평가) 조립
   [상태] pass

9b. (같은 세션) "내용평가는" : 정상
   [규칙] metric — content_eval, 세션 메모리의 최근 제목("늑대와 어린양") 사용
   [원인] 9번에서 title_card가 세션에 최근 제목을 저장 → 멀티턴 단축 질문이 정확히 그 제목으로 응답
   [상태] pass

10. "박쥐와 족제비 얘기하고" : 정상 (9번과 동일 패턴)
    [규칙] title_card
    [상태] pass

10b. (같은 세션) "원문 보여줘" : 정상
    [규칙] verbatim — origin, 세션 메모리의 최근 제목("박쥐와 족제비") 사용
    [원인] 제목 없이도 memory 최근 제목의 원문을 정확히 반환
    [상태] pass

11. "제일 재밌는 우화가 뭐야" : 정상 ✅ (과거 [P0 환각] 항목 — 지금은 해결 확인됨)
    [규칙] catalog — `try_handle_fun_rank_question` (`_FUN_RANK_MARKERS`)
    [원인] 메타(fun 점수) 기준 Top-3 나열, LLM 없음. 예전엔 유사도 0.000 상태로 "토끼와 거북이"를 환각 답변했었음
    [상태] pass

12. "결말톤이 새드인 우화 있어?" : 기대이하 (신규 발견 패턴)
    [규칙] metric — ending_tone (`detect.py` "결말톤" 키워드에 걸림), 제목 없음 → 되묻기
    [원인] 질문 의도는 "특정 값(새드)으로 우화를 **필터링**"인데, "결말톤" 키워드가 먼저 매칭되어 "어떤 우화의 결말톤을 알고 싶으신가요?"처럼 **단일 제목 지표 질의**로 오인됨. `catalog.py`엔 재미도 Top-N(`try_handle_fun_rank_question`)만 있고, 다른 지표(폭력성/교훈명확도/결말톤 등)를 값으로 필터링하는 규칙이 없음 — 11번과 대칭되는 기능 공백
    [해결] 키워드 나열이 아니라, "메트릭명 + (이|가)? + (값 형용사|구체값) + 우화 + (있어|뭐야|알려줘)" 형태의 **정규식 패턴**으로 감지: 예) `r"(재미도|폭력성|교훈\s*명확도|결말톤)\s*(가|이)?\s*([가-힣]+)\s*(인|높은|낮은)\s*우화"`. 매칭되면 `detect_metric_kind`가 이미 알고 있는 kind와 캡처된 값(새드/해피, 또는 임계값)으로 `list_fable_metas`를 필터링 — `try_handle_fun_rank_question`과 같은 구조를 재사용해 "메트릭 조건 필터" 함수 하나로 일반화하는 방향 제안 (지금 당장 구현하라는 뜻은 아님, 의견만)
    [상태] x (해결 대기 — 의견만, 코드 미변경)

13. "박쥐와 족제비 한마디 결론" (MBTI 미설정) : 정상
    [규칙] verbatim — modern (`detect_verbatim_content_type` "한마디 결론"), MBTI 없음 → 문서 그대로
    [상태] pass

14. "MBTI : enfp" : 정상
    [규칙] mbti_commands — `_SET_PATTERN` (`^MBTI\s*[:：=]\s*[IE][NS][TF][JP]$`)
    [원인] 정규식 완전일치 → 세션에 ENFP 저장
    [상태] pass

15. (같은 세션) "박쥐와 족제비 유형별 해석" : 정상
    [규칙] verbatim — modern + 세션 MBTI 있음 → `reinterpret_fable_for_mbti` (LLM)
    [원인] 14번에서 저장한 ENFP를 이어받아 유형별 재해석 생성
    [상태] pass

16. "mbti 변경" : 정상 ✅ (과거 [x] 대기 항목 — 지금은 해결 확인됨)
    [규칙] mbti_commands — `_HELP_NORMALIZED` 정확 일치("mbti변경")
    [원인] 공백 제거 정규화 후 헬프 세트에 포함 → 설정 안내 템플릿 응답
    [상태] pass

17. "MBTI : 그냥" : 기대이하 (신규 발견 패턴)
    [규칙] 전부 미스 → ② Agent(Groq) — "MBTI에 대해 더 구체적인 질문을 해주세요."
    [원인] `_SET_PATTERN`은 값 부분이 정확히 `[IE][NS][TF][JP]` 4글자일 때만 매칭 → "그냥"은 불일치로 통과 못 함. 이어지는 `_is_mbti_help_request`도 "mbti:그냥"이 `_HELP_SUBSTRINGS`의 어떤 문자열("mbti설정", "mbti변경" 등)도 포함하지 않아 실패. `vague._is_too_vague`도 콜론(`:`)이 제거 대상 문자에 없어 compact 길이가 4를 넘어(7자) 걸리지 않음 → 결국 아무 규칙에도 안 걸리고 Groq이 문서 근거 없이 일반론 답변. 사용자는 분명히 "MBTI :" 형식으로 설정을 시도했는데, 형식 오류 안내를 못 받는 셈
    [해결] `_SET_PATTERN`을 두 단계로: (1) 기존 정확 매칭 유지 (2) **접두만 일치하는 완화 정규식** 하나 추가 — `r"^\s*MBTI\s*[:：=]\s*"`로 먼저 걸러낸 뒤, 값이 유효한 4글자 코드가 아니면 "입력하신 값이 올바른 MBTI 유형이 아닙니다. 예: MBTI : infp" 같은 전용 안내를 반환. 키워드를 더 추가하는 방식이 아니라 **"MBTI: 로 시작하지만 값이 틀린 경우"라는 조건 자체를 정규식으로 분리 포착**하는 방향
    [상태] x (해결 대기 — 의견만, 코드 미변경)

18. "mbti로 바꿔 보려면" : 정상 ✅ (과거 [x] 대기 항목 — 지금은 해결 확인됨)
    [규칙] mbti_commands — `_HELP_SUBSTRINGS`의 "mbti로바꿔" 부분일치
    [상태] pass

19. "mbti 수정" : 정상 ✅ (과거 [x] 대기 항목 — 지금은 해결 확인됨)
    [규칙] mbti_commands — `_HELP_NORMALIZED`/`_HELP_SUBSTRINGS` "mbti수정"
    [상태] pass

20. "아버지와 아들들 오늘날로 치면" : 정상
    [규칙] verbatim — modern (`detect_verbatim_content_type` "오늘날로 치면", 하위호환 라벨)
    [원인] MBTI 없음 → 문서에 저장된 modern 텍스트 그대로 반환 (LLM 아님)
    [상태] pass

21. "아버지와 아들들 그대로 읽어줘" : 정상 ✅ (과거 [x] 대기 항목 — 지금은 해결 확인됨)
    [규칙] verbatim — origin ("그대로"/"읽어" 트리거, `verbatim.py` line 57~58)
    [원인] 과거엔 "그대로"/"읽어줘"가 트리거가 아니어서 Groq이 환각했었음. 지금은 정상적으로 원문 그대로 반환
    [상태] pass

================================================================================

## [상태] x — 다음에 고칠 것 (의견만, 미착수)

| 순번 | 질문 유형 | 문제 | 제안 방향(정규식 기반) |
|---|---|---|---|
| 12 | 메트릭 값으로 우화 필터링 ("결말톤이 새드인 우화 있어?") | "결말톤" 키워드가 먼저 매칭돼 단일 제목 질의로 오인, 되묻기만 함 | `(메트릭명)\s*(가/이)?\s*(값)\s*(인/높은/낮은)\s*우화` 패턴 신규 인식 → `list_fable_metas` 필터. `try_handle_fun_rank_question`을 일반화하는 방향 |
| 17 | MBTI 형식은 맞지만 값이 틀림 ("MBTI : 그냥") | 정확 매칭 실패 시 안내 없이 Groq 일반론으로 흘러감 | `^\s*MBTI\s*[:：=]\s*` 접두 전용 정규식으로 "설정 시도"만 먼저 감지 → 값 검증 실패 시 전용 안내 문구 반환 |

## 새로 발견된 패턴 (목록에 없던 표현)

- 없음. 지시문의 21개 + 멀티턴 후속 2개(9b, 10b) 외에 별도로 추가한 케이스는 없음 — 12번과 17번은 지시문에 있던 질문에서 나온 기대이하이지 새 질문을 만든 것은 아님

## 과거 감사표 대비 변화 요약

`질문 테스트 결과.txt`(2026-07-20 작성, 사람 테스트)에서 [미구현]/[부분]이던 항목 중 이번 라이브 테스트로 **해결이 확인된 것**:
- title_card "제목+얘기하고" (과거 12번 P0 환각) → 9, 10번에서 정상 확인
- catalog fun Top-N (과거 14번 P0 환각) → 11번에서 정상 확인
- mbti_commands 자연어 안내 확장 (과거 7·8번, P1) → 16, 18, 19번에서 정상 확인
- verbatim "그대로/읽어" 트리거 (과거 17번) → 21번에서 정상 확인

이번에 **새로 드러난** 기대이하는 12번(메트릭 필터), 17번(MBTI 형식 오류 안내) 두 건 — 둘 다 코드는 손대지 않았고 위 표의 의견만 남김.

================================================================================
